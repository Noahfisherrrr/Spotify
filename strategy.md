/project-root
│
├── /dist               # FOLDER: Where the browser-ready files go (Compiled)
│   ├── /css            # Final compiled styles (style.css)
│   ├── /js             # Final scripts
│   └── /assets         # Final images/fonts
│
├── /src                # FOLDER: Where you actually work (Source)
│   ├── /scss           # SCSS architecture
│   │   ├── /base       # _reset.scss, _typography.scss
│   │   ├── /components # _sidebar.scss, _navbar.scss, _card.scss,
│   │   ├── /layout     # _homepage.scss , _search.scss 
│   │   ├── /abstracts  # _variables.scss, _mixins.scss
│   │   └── main.scss   # The "1" that @imports everything else
│   │
│   └── pages           # homepage.html , search.html
│
├── index.html          # Main Page (homepage)
└── main.scss      # SaaS App Page

Dev keywords: 
    add: New files added;
    fix: Buggs fixed;
    
Noah: 
    File Stucture(fonts,folders,reset etc.);
    Navigation bar(Mobile/desktop);
    Footer Player(Mobile/desktop);
    Homepage (Mobile/desktop);


Sandro:
    Library(Mobile/desktop);
    Presentation of Project(Powerpoint);


Giorgi:
    Search Page(Mobile/desktop);
    README.md(taking care of it);